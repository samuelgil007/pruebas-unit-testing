name: CI
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Install modules
      run: npm install
  test:
    runs-on: ubuntu-latest
    steps:
    - name: Run tests
      run: npm run coverage
      
